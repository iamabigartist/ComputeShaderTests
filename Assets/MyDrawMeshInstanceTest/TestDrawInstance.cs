using UnityEngine;
namespace MyDrawMeshInstanceTest
{
    public class TestDrawInstance : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}
